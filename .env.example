# Copy to `.env` or `.env.local` and fill in values. Never commit real secrets.
# See: https://nextjs.org/docs/app/building-your-application/configuring/environment-variables

# Maintainers only — required to run the Next app (dev/build/start). Not needed for npm test.
# FRAMEHUB_MAINTAINER=1
# VOIDFORGE_MAINTAINER=1

# SQLite — must match the file the app opens (see src/lib/sqlite-path.ts)
# Default when unset: ./dev.db in project root
# start.sh also sets SQLITE_DATABASE_PATH to the resolved absolute path at boot.
DATABASE_URL=file:./dev.db

# Session signing — required in production (use a long random string)
AUTH_SECRET=

# Public site URL for auth redirects and cookies (set in production to https://void-forge.org)
AUTH_URL=

# Optional: public URL for emails and client-visible absolute links (https://void-forge.org)
NEXT_PUBLIC_APP_URL=

# Google OAuth (optional if you only use email/password)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Resend — email (optional; verification / opt-in report notify / newsletter skip if unset)
RESEND_API_KEY=
# From addresses (must be verified in Resend). Keep support and news separate.
# EMAIL_FROM_SUPPORT=Voidforge <support@void-forge.org>
# EMAIL_FROM_NEWSLETTER=Voidforge News <news@void-forge.org>

# Optional: comma-separated emails that always receive admin on sign-in (not stored in git)
AUTH_ADMIN_EMAILS=

# Buy Me a Coffee webhook signing secret (for supporter badge automation)
BMC_WEBHOOK_SECRET=

# Optional: comma-separated hostnames allowed when reading X-Forwarded-Host
# TRUSTED_HOSTS=void-forge.org,www.void-forge.org
TRUSTED_HOSTS=

# Optional: stable Server Action encryption across deploys (generate once: openssl rand -base64 32)
# NEXT_SERVER_ACTIONS_ENCRYPTION_KEY=

# Player Sync — Warframe Arsenal Twitch extension auth (server-side only)
# Install https://www.warframe.com/en/twitch-extension on YOUR Twitch channel, then set
# its numeric channel ID (Creator Dashboard → Settings → Channel → About, or Helix users API).
# TWITCH_ARSENAL_CHANNEL_ID=
# Optional override: paste a fresh extension JWT instead of resolving via channel ID
# WARFRAME_ARSENAL_JWT=

# Voidforge Discord bot (world-state alerts + /bot dashboard)
# Create an app at https://discord.com/developers/applications
# OAuth2 redirect: {AUTH_URL}/api/bot/discord/callback  (e.g. http://localhost:3000/api/bot/discord/callback)
# Bot scopes: bot, applications.commands
# User OAuth scopes for linking: identify, guilds
# DISCORD_BOT_TOKEN=
# DISCORD_CLIENT_ID=
# DISCORD_CLIENT_SECRET=
# Optional: worldstate poll interval in ms (default 60000)
# BOT_WORLDSTATE_POLL_MS=60000
